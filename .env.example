# Census Bureau API key — request at https://api.census.gov/data/key_signup.html
CENSUS_API_KEY=your_census_api_key_here

# PostgreSQL / PostGIS connection (optional, used if DB backend is enabled)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=healthcare_access
DB_USER=postgres
DB_PASSWORD=your_password_here
